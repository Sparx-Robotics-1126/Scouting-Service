# Autogenerated bulkloader.yaml file.
# At a minimum address the items marked with TODO:

# If you have module(s) with your model classes, add them here. Also
# change the kind properties to model_class.
python_preamble:
- import: base64
- import: re
- import: google.appengine.ext.bulkload.transform
- import: google.appengine.ext.bulkload.bulkloader_wizard
- import: google.appengine.ext.db
- import: google.appengine.api.datastore
- import: google.appengine.api.users

transformers:

- kind: Scouting
  connector: csv
#  connector_options:
#    export_options:
#      dialect: excel
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: auto.ballsAcquired
      external_name: auto.ballsAcquired
      # Type: Integer Stats: 7 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: auto.ballsScoredHigh
      external_name: auto.ballsScoredHigh
      # Type: Integer Stats: 7 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: auto.ballsScoredHotHigh
      external_name: auto.ballsScoredHotHigh
      # Type: Integer Stats: 7 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: auto.ballsScoredHotLow
      external_name: auto.ballsScoredHotLow
      # Type: Integer Stats: 7 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: auto.ballsScoredLow
      external_name: auto.ballsScoredLow
      # Type: Integer Stats: 7 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: auto.ballsScored
      external_name: auto.ballsScored
      # Type: Integer Stats: 7 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: auto.ballsShot
      external_name: auto.ballsShot
      # Type: Integer Stats: 7 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: auto.endingLocationX
      external_name: auto.endingLocationX
      # Type: Float Stats: 7 properties of this type in this kind.
      import_transform: transform.none_if_empty(float)

    - property: auto.endingLocationY
      external_name: auto.endingLocationY
      # Type: Float Stats: 7 properties of this type in this kind.
      import_transform: transform.none_if_empty(float)

    - property: auto.startedWithBall
      external_name: auto.startedWithBall
      # Type: Boolean Stats: 7 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: auto.startingLocationX
      external_name: auto.startingLocationX
      # Type: Float Stats: 7 properties of this type in this kind.
      import_transform: transform.none_if_empty(float)

    - property: auto.startingLocationY
      external_name: auto.startingLocationY
      # Type: Float Stats: 7 properties of this type in this kind.
      import_transform: transform.none_if_empty(float)

    - property: eventKey
      external_name: eventKey
      # Type: String Stats: 7 properties of this type in this kind.

    - property: general.numberOfPenalties
      external_name: general.numberOfPenalties
      # Type: Integer Stats: 7 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: general.numberOfTechnicalFouls
      external_name: general.numberOfTechnicalFouls
      # Type: Integer Stats: 7 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: general.playsDefense
      external_name: general.playsDefense
      # Type: Boolean Stats: 7 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: matchKey
      external_name: matchKey
      # Type: String Stats: 7 properties of this type in this kind.

    - property: nameOfScouter
      external_name: nameOfScouter
      # Type: String Stats: 7 properties of this type in this kind.

    - property: teamKey
      external_name: teamKey
      # Type: String Stats: 7 properties of this type in this kind.

    - property: tele.ballsAcquiredFromFloor
      external_name: tele.ballsAcquiredFromFloor
      # Type: Integer Stats: 7 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: tele.ballsAcquiredFromHuman
      external_name: tele.ballsAcquiredFromHuman
      # Type: Integer Stats: 7 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: tele.ballsCaughtOverTruss
      external_name: tele.ballsCaughtOverTruss
      # Type: Integer Stats: 7 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: tele.ballsThrownOverTruss
      external_name: tele.ballsThrownOverTruss
      # Type: Integer Stats: 7 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: tele.completedAssistsFromFloor
      external_name: tele.completedAssistsFromFloor
      # Type: Integer Stats: 7 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: tele.completedAssistsFromHuman
      external_name: tele.completedAssistsFromHuman
      # Type: Integer Stats: 7 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: tele.scoredHigh
      external_name: tele.scoredHigh
      # Type: Integer Stats: 7 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: tele.scoredLow
      external_name: tele.scoredLow
      # Type: Integer Stats: 7 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: tele.shotHigh
      external_name: tele.shotHigh
      # Type: Integer Stats: 7 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

    - property: tele.shotLow
      external_name: tele.shotLow
      # Type: Integer Stats: 7 properties of this type in this kind.
      import_transform: transform.none_if_empty(int)

